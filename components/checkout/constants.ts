/**
 * The shipping option that represents collecting the order from the Bedford
 * Hill store rather than having it delivered. It is a real ShippingOption row
 * on the backend, so checkout treats it like any other option — but the UI
 * has to single it out to show the date/time picker instead of a courier.
 */
export const STORE_PICKUP_OPTION_ID = 34;
